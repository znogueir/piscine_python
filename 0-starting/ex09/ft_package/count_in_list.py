from typing import Any


def count_in_list(lst: list, elem: Any):
    """
    Takes a list and an element as parameters.
    Returns the number of times the element appears in the list.
    """

    return lst.count(elem)
